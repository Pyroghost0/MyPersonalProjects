using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReflectionQuestions : MonoBehaviour
{
    /* 1) My SpeedPowerUp class inherits the EnemyPowerUp class
     * 
     * 2) My TextElements class impliments the ChangesTextLetter interface
     * 
     * 3) My PowerUp classes (ex. SpeedPowerUp) inherits from the super-type EnemyPowerUp class through polymorphism
     * 
     * 4) You would need to find the game manager to use it because without it, the computer doesn't know what you want
     */
}
